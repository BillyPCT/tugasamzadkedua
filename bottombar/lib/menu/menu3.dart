import 'package:flutter/material.dart';

class menu3 extends StatelessWidget {
  const menu3({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text("Ini Menu ketiga"),
        ),
      ),
    );
  }
}