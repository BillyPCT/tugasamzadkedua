import 'package:flutter/material.dart';

class MobileLeged extends StatelessWidget {
  const MobileLeged({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}